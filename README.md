# multipart-stream
Sends multiple files as one stream with support for custom headers per file.

## Install
Python venv or other isolated environment recommended.
```bash
npm i
pip install -r requirements.txt
```

Run server:
```bash
npm start
```

Send dummy request to server:
```bash
npm run request
```
